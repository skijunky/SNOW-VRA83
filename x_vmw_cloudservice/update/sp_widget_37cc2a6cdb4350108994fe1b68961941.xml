<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>custom</category>
        <client_script><![CDATA[function($scope) {
	var c = this;
	var data = JSON.parse(JSON.stringify(c.data));
	$scope.page.g_form.getField(data.field_id).duallist_data = c.data;
	var dirty = false;
	var interval = 0;
	
	$scope.page.g_form.setMandatory(data.field_id, false);
	
	interval = setInterval(function(){
		var duallistContainerElement = $("#"+'duallist_container'+c.data.field_id)[0];
		if(duallistContainerElement){
			clearInterval(interval);
			if(data.all_options.type=="scriptAction" || data.default_value.type=="scriptAction"){
				getDuallistData();
			}
			else{
				initializeDuallist();
			}
		}
	}, 1000);
	
	function getDuallistData(){
		var getDualListDataInterval=0;
		clearInterval(getDualListDataInterval);
		var getDualListDataInterval = setInterval(function(){
			var all_options;
			var default_value;
			if(data.all_options.type=="scriptAction"){
				all_options = $scope.page.g_form.getField(data.field_id).all_options_onload;
			}
			else{
				all_options = data.all_options;
			}
			if(data.default_value.type=="scriptAction"){
				default_value = $scope.page.g_form.getField(data.field_id).default_value_onload;
			}
			else{
				default_value = (data.default_value||[]);
			}
			
			all_options = (all_options||[]);
			default_value = (default_value||'');

			if(all_options.length){
				if(default_value){
					all_options = all_options.map(function(option){
						return typeof option.label == 'undefined'?{value:option,label:option}:option;
					})
					data.all_options = all_options;
					data.default_value = default_value;
					clearInterval(getDualListDataInterval);
					initializeDuallist();
				}
			}
		},1000);

	}
	
	function initializeDuallist(){

		$('#data_json'+c.data.field_id).val(JSON.stringify(data.options));
		//handelling read only---------------------------------------
		if(data.constraints.read_only){
			$("#duallist-container"+data.field_id).css("display","none");
			$("#read_only"+data.field_id).css("display","block");
			data.selected_options = (getOptionsFromValues(data.default_value) ||[]);
			var selected_options ='';
			var tableRows = "";
			data.selected_options.forEach(function(option){
				selected_options+='<tr><td>'+option.label+'</td></tr>';
			});
			$("#read_only"+data.field_id+" tbody").html(selected_options);
			updateAnswer(data.default_value);
		}
		else{
			refillDuallist([].concat(data.default_value));
		}

		//handelling button and click functionalities---------------------------------
		//making dirty true if clicked
		$("#duallist_container"+data.field_id).mousedown(function(){
			data.dirty = true;
		})
		//handelling select button event
		$("#select_option_button" + data.field_id).click({param1:"unselected"+data.field_id},function(event){
			selectOptions(event.data.param1);
		});
		//handelling unselect button event
		$("#unselect_option_button" + data.field_id).click({param1:"selected"+data.field_id},function(event){
			unselectOptions(event.data.param1);
		});
		//handelling select all button event
		$("#select_all_options_button" + data.field_id).click({param1:"unselected"+data.field_id},function(event){
			selectAllOptions(event.data.param1);
		});
		//handelling unselect all button event
		$("#unselect_all_options_button" + data.field_id).click({param1:"selected"+data.field_id},function(event){
			unselectAllOptions(event.data.param1);
		});	

	}


	//value to option
	function getOptionFromValue(value){
		return (data.all_options || []).filter(function(item){
			return item.value == value;
		})[0];
	}
	function getOptionsFromValues(values){
		return (values || []).map(function(value){
			return getOptionFromValue(value);
		});
	}


	//option to value
	function getValuesFromOptions(options){
		return (options || []).map(function(option){
			return option.value;
		});
	}


	//option selection and removal
	function selectOptions(id){
		var values = $("#"+ id).val();
		if(!values)return;
		var selectedValues = (values.concat(getValuesFromOptions(data.selected_options)) || []);
		refillDuallist(selectedValues);
	}
	function unselectOptions(id){
		var selectedValues = getValuesFromOptions(data.selected_options);
		var values = $("#"+ id).val();
		if(!values)return;
		values.forEach(function(value){
			selectedValues.splice(selectedValues.indexOf(value),1);
		});
		refillDuallist(selectedValues);
	}
	function selectAllOptions(id){
		var values = $("#"+ id +" option").map(function(){
			return this.value;
		}).get();
		if(!values)throw "Something went wrong while selecting all option";
		var selectedValues = values.concat(getValuesFromOptions(data.selected_options))
		refillDuallist(selectedValues);
	}
	function unselectAllOptions(id){
		var selectedValues = [];
		refillDuallist(selectedValues);
	}


	//adds optin to a select element
	function addOptionsToSelectElement(id,options){
		(options || []).forEach(function(option){
			var optionElement = new Option(option.label, option.value);
			$(optionElement).html(option.label);
			$("#"+id).append(optionElement);
		});
	}

	//refills dual list data
	function refillDuallist(selectedValues){
		var selected_options = getOptionsFromValues(selectedValues);
		var unselected_options = (data.all_options || []).filter(function(option, index){
			return !selectedValues.filter(function(value,index){return option.value==value;}).length;
		});
		
		data.minMaxFulfilled = true;
		if(data.constraints['max-value']){
			if(selected_options.length>data.constraints['max-value']){
				$("#error-message"+data.field_id).text("Maximum value can be "+ data.constraints["max-value"]);
				data.minMaxFulfilled = false;
			}

		}
		if(data.constraints['min-value']){
			if(data.constraints['min-value'] > selected_options.length){
				$("#error-message"+data.field_id).text("Minimum value can be "+ data.constraints["min-value"]);
				data.minMaxFulfilled = false;
			}
		}

		if(data.minMaxFulfilled) $("#error-message"+data.field_id).text(" ");
		
		data.selected_options = selected_options;
		data.unselected_options = unselected_options;
		$("#selected"+ data.field_id).empty();
		$("#unselected"+ data.field_id).empty();
		data.selected_options.sort(function(a,b){
			if( b.label >a.label)return -1;
			if(a.label>b.label)return 1;
			return 0;
		});
		data.unselected_options.sort(function(a,b){
			if(b.label > a.label)return -1;
			if(a.label>b.label)return 1;
			return 0;
		});

		addOptionsToSelectElement("selected"+data.field_id, data.selected_options);
		addOptionsToSelectElement("unselected"+data.field_id, data.unselected_options);
		updateAnswer(getValuesFromOptions(data.selected_options));

	}

	//updates answer to the hidden text field
	function updateAnswer(answer){
		if(!answer)return;
		if(answer.length){
			$("#answer"+data.field_id).val(JSON.stringify(answer));
			$scope.page.g_form.clearOptions(data.field_id);
			$scope.page.g_form.addOption(data.field_id, answer, answer);
			$scope.page.g_form.setValue(data.field_id, answer);
			if(data.dirty){
				$("#duallist_container"+data.field_id).css("border","none");
			}
			$scope.page.g_form.getField(data.field_id).answer = answer;
			if(data.minMaxFulfilled){
				$("#selected"+data.field_id).css("border","");
				$("#unselected"+data.field_id).css("border","");
			}
			else{
				$("#selected"+data.field_id).css("border","1px solid #c83c36");
				$("#unselected"+data.field_id).css("border","1px solid #c83c36");
			}
		}
		else{
			$("#answer"+data.field_id).val("[]");
			$scope.page.g_form.clearOptions(data.field_id);
			$scope.page.g_form.addOption(data.field_id, [], []);
			if(data.constraints.required || !data.minMaxFulfilled){
				$("#selected"+data.field_id).css("border","1px solid #c83c36");
				$("#unselected"+data.field_id).css("border","1px solid #c83c36");
			}
			$scope.page.g_form.getField(data.field_id).answer = answer;
			
		}
	}
}

	]]></client_script>
        <controller_as>c</controller_as>
        <css>.dual-list .list-group {&#13;
    margin-top: 8px;&#13;
}&#13;
&#13;
.list-left li, .list-right li {&#13;
    cursor: pointer;&#13;
}&#13;
&#13;
.list-arrows {&#13;
    padding-top: 100px;&#13;
}&#13;
&#13;
.list-arrows button {&#13;
  margin-bottom: 20px;&#13;
}&#13;
.cas-dual-list{&#13;
	height:150px;&#13;
  background-color:#fff;&#13;
  border-radius:.125rem;&#13;
  padding-top:2px;&#13;
  border:1px solid #ccc;&#13;
}&#13;
&#13;
.cas-dual-list:hover{&#13;
	background-color:#fff;&#13;
}&#13;
.btn{&#13;
	line-height:1;&#13;
  background-color: transparent;&#13;
  margin:.25rem .5rem .25rem 0;&#13;
}&#13;
&#13;
.cas-duallist-table-container{&#13;
 	 padding:0;height:auto&#13;
}&#13;
&#13;
.cas-duallist-table{&#13;
 	margin-top: 0px;&#13;
  margin-bottom: 0px;&#13;
}&#13;
</css>
        <data_table>sp_instance</data_table>
        <demo_data/>
        <description>For duallist dynamic data support, service portal</description>
        <docs/>
        <field_list/>
        <has_preview>false</has_preview>
        <id>cas_dual_list</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {
  
}]]></link>
        <name>CAS_Duallist</name>
        <option_schema/>
        <public>false</public>
        <roles>x_vmw_cloudservice.vra_end_user,x_vmw_cloudservice.vra_catalog_admin</roles>
        <script><![CDATA[(function() {
	data.options = options;
	data.all_options = (options.valueList ||[]);
	if(typeof data.all_options[0] != "object" && !data.all_options.id){
		data.all_options = data.all_options.map(function(item){
			return {"value":item,"label":item};
		});
	}
	data.default_value = (options["default"]||[]);
	data.field_id = options.field_id;
	data.constraints = options.constraints;
})();]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>admin</sys_created_by>
        <sys_created_on>2020-09-01 12:10:56</sys_created_on>
        <sys_id>37cc2a6cdb4350108994fe1b68961941</sys_id>
        <sys_mod_count>165</sys_mod_count>
        <sys_name>CAS_Duallist</sys_name>
        <sys_package display_value="VMware vRealize Automation ITSM Application 8.3" source="x_vmw_cloudservice">0d6a0ec1db11bb407c83712ebf96194d</sys_package>
        <sys_policy/>
        <sys_scope display_value="VMware vRealize Automation ITSM Application 8.3">0d6a0ec1db11bb407c83712ebf96194d</sys_scope>
        <sys_update_name>sp_widget_37cc2a6cdb4350108994fe1b68961941</sys_update_name>
        <sys_updated_by>admin</sys_updated_by>
        <sys_updated_on>2020-10-26 07:18:37</sys_updated_on>
        <template><![CDATA[<div id="{{'duallist_container'+c.data.field_id}}">
  <div>
    <span id="{{'error-message'+c.data.field_id}}" style="font-size:15px;font-weight:300;color:#c83c36"></span>
  </div>
  <div class="row" id="{{'duallist-container'+c.data.field_id}}">  
    <div class="col-xs-5">
      <select name="from" id="{{'unselected'+c.data.field_id}}" class="cas-dual-list form-control" size="8" multiple="multiple">      
      	<option value="">Loading...</option>
      </select>
    </div>

    <div class="col-xs-2">
      <button type="button" id="{{'select_option_button'+c.data.field_id}}"  class="btn btn-block"><cas-clr-icon shape="angle" size="16" dir="right" ></cas-clr-icon></button>
      <button type="button" id="{{'unselect_option_button'+c.data.field_id}}"  class="btn btn-block"><cas-clr-icon shape="angle" size="16" dir="left" ></cas-clr-icon></button>
      <button type="button" id="{{'select_all_options_button'+c.data.field_id}}"  class="btn btn-block"><cas-clr-icon shape="angle-double" size="16" dir="right" ></cas-clr-icon></button>
      <button type="button" id="{{'unselect_all_options_button'+c.data.field_id}}" class="btn btn-block"><cas-clr-icon shape="angle-double" size="16" dir="left" ></cas-clr-icon></button>
    </div>

    <div class="col-xs-5">
      <select name="to" id="{{'selected'+c.data.field_id}}" class="cas-dual-list form-control" size="8" multiple="multiple">
        <option value="">Loading...</option>
      </select>
    </div>
  </div>
  
  <div class="row" id="{{'read_only'+c.data.field_id}}" style="display:none;">
    <div  class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
      <div  class="form-control cas-duallist-table-container" >
        <table  class="table table-compact cas-duallist-table" >
          <tbody >
            <!--<tr>
              <td>First Item</td>
            </tr>
            <tr>
              <td>Second Item</td>
            </tr>
            <tr>
              <td>Third Item</td>
            </tr>-->
          </tbody>
        </table>
      </div>
    </div>
  </div>
  <input type="text" id="{{'answer'+c.data.field_id}}" style="visibility:hidden"/>
  <input type="text" id="{{'all_options'+c.data.field_id}}" style="visibility:hidden"/>
  <input type="text" id="{{'default_value'+c.data.field_id}}" style="visibility:hidden"/>
  <input type="text" id="{{'data_json'+c.data.field_id}}" style="visibility:hidden"/>
  <script>
  </script>
</div>]]></template>
    </sp_widget>
</record_update>
